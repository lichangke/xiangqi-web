# Task Bundle D-2.3 真实页面人工联调记录

## 文档头信息
- 文档名称：Task Bundle D-2.3 真实页面人工联调记录
- 当前状态：已确认
- 所属阶段：Review 补充验收记录
- 所属项目：xiangqi-web / 网页版中国象棋项目
- 所属功能 / 子功能：Task Bundle D-2.3（真实 narrative 模型接入最小闭环）
- 上游文档：review.md, sdd-status.md, execution-contract.md, docs/Task Bundle D-2.3 实现交接.md
- 创建时间：2026-04-07
- 最后更新时间：2026-04-08

## 1. 记录目的
本记录不是回退 D-2.3 的 review 结论，也不是重新判断“是否已收口”。

本记录的目的只有一个：
> 在 D-2.3 已完成自动化验证、review 回写、本地稳定快照与远端同步之后，再补一份**真实页面层人工联调留痕**，把“route 级最小闭环已成立”补成“页面体验链路也有验收证据”。

因此，本记录属于：
- **D-2.3 收口后的补充验收留痕**
- **低成本补记**
- **不扩写范围，不开启新切片**

---

## 2. 当前边界
### 明确要验证
1. 前端真实页面在已有 D-2.3 代码状态下，能正常承接 narrative runtime 结果
2. 用户完成一回合后，timeline 可正常展示 narrative 结果
3. narrative provider 正常时，不破坏既有页面主链路
4. 若 provider 不可用或未命中真实生成，页面侧 fallback 体验不应断裂

### 明确不验证
- decision 模型真实外接
- 完整 secret 持久化 / 加密方案
- 完整 provider 平台治理
- 并发治理平台
- 新一轮 D-2.4 范围判断

---

## 3. 走查环境
- 记录方式：最小人工联调 / 页面层走查
- 走查目标：只确认 D-2.3 页面体验链路未跑偏
- 走查口径：以“能否形成页面层验收留痕”为准，不做额外产品打磨
- 走查时间：2026-04-07
- 测试人：用户 + 主会话配合记录
- 访问入口：用户端与管理端外部访问地址均已可打开
- 当前后台配置实况：
  - 用户端与管理端均可登录
  - 用户端可正常进入对局并完成真实落子
  - 当前后台 `decision` 配置已存在且启用
  - 当前后台 `narrative` 配置仍为空且未启用（`narrativeConfigured=false`）
  - 因此本次页面联调实际验证到的是：**页面主链路 + narrative fallback 链路成立**；**未验证到“页面层命中真实 narrative provider”**

---

## 4. 建议最小走查项
### D23-01 打开页面并进入可对局状态
- 操作：启动当前应用，进入用户端页面，确保可以进入一局对局
- 期望：页面可正常打开，无阻断性报错；棋盘、时间线 / 讨论区、基础操作区可见
- 当前记录：**通过**。用户端与管理端均可正常打开并登录；用户端可进入对局页，棋盘、时间线 / 讨论区与基础操作区可见，无阻断性报错。

### D23-02 完成一回合并观察页面 narrative 展示
- 操作：完成一次合法用户落子，等待系统返回 AI 应对
- 期望：页面能出现新的回合 narrative / timeline 项；结构不空白、不报错、不打断棋局推进
- 当前记录：**通过**。用户确认用户端可以正常下棋子，页面对局链路成立；回合推进与演绎展示未出现“空白 / 报错 / 主链路中断”的阻断性问题。

### D23-03 核对 narrative 命中真实链路时的页面表现
- 操作：在 narrative provider 前提成立的情况下完成一回合
- 期望：页面正常展示 narrative 结果；即使底层来源为 provider，页面契约与展示层不应出现异常抖动或结构错位
- 当前记录：**本次未通过 / 准确说是未具备验证前提**。当前后台管理接口返回：`decision` 已配置并启用，但 `narrative` 仍为空且未启用（`narrativeConfigured=false`）。因此用户在真实页面看到的“对局和演绎的 AI 并没有接入大模型”，其根因不是页面契约或 D-2.3 route 链路回退，而是**页面联调当下并不满足 narrative provider 命中前提**，所以本次没有形成“页面层命中真实 narrative provider”的人工证据。

### D23-04 核对 fallback 语义在页面侧不破链
- 操作：在 provider 不可用、未命中或回退场景下观察一回合结果
- 期望：页面仍能展示 fallback narrative；用户感知是“仍可继续对局”，而不是时间线空白或主链路中断
- 当前记录：**通过**。当前页面实测恰好处在 `narrative` 未配置 / 未启用场景，用户端仍可登录、下棋、推进对局并看到演绎结果，说明 fallback narrative 在页面侧不破链，本轮最关心的“主链路不因真实接入条件缺失而变脆”已被页面层人工印证。

### D23-05 核对当前补记是否足以作为页面层留痕
- 操作：综合 D23-01 ~ D23-04
- 期望：若页面主链路正常、narrative 展示成立、fallback 不断裂，则本记录可判定为“补记完成”
- 当前记录：**部分通过 / 可作为补记完成**。当前已经形成“页面主链路正常 + narrative fallback 页面链路成立”的人工留痕；但尚未形成“页面层命中真实 narrative provider”的人工留痕，因此本记录应以“部分通过、保留条件项”收口，而不是写成页面层 provider 命中已被人工验证。

---

## 5. 当前已知自动化依据（非人工结论）
以下内容**不是**本记录的人工联调结果，只是为了说明为什么本次补记可以保持轻量：

- `review.md` 已确认：D-2.3 在约定边界内“有条件通过 / 阶段性通过”
- `sdd-status.md` 已确认：D-2.3 的 review 回写与本地稳定快照已完成，但远端最新提交尚未同步
- 当前已落盘自动化验证包括：
  - `npm test` 通过（30/30）
  - `npm run build` 通过
  - route 级 provider 回归测试已补齐
  - `/api/narrative/resolve` 在 compact response shape 下可返回 `source=provider`
  - `fallbackUsed=false` 路径已被验证

因此，本记录不承担“证明 D-2.3 是否成立”的职责，只承担“补一份页面层人工留痕”的职责。

---

## 6. 结果区（本轮人工联调结论）
### 总体结果
- [ ] 通过
- [ ] 不通过
- [x] 部分通过
- [ ] 待执行

### 结果说明
本轮真实页面人工联调已经补齐了一个关键事实：
- **用户端与管理端真实页面可登录**
- **用户端真实页面可进入对局、正常落子并继续推进时间线**
- **当前 narrative 在页面层即使未命中真实 provider，fallback 也不会打断主链路**

但本轮同时也明确暴露出一个边界事实：
- 当前后台 `narrative` 配置并未真正启用，接口返回 `narrativeConfigured=false`
- 因此用户在页面侧感知到“演绎 AI 并没有接入大模型”是符合当前配置现状的
- 所以本次不能把人工联调结果写成“页面层真实 provider 命中已验证通过”

换句话说：
> **本次页面联调证明了 D-2.3 的页面承接与 fallback 语义成立，但没有证明页面层真实 provider 命中已成立。**

### 分项记录
- D23-01：通过
- D23-02：通过
- D23-03：未具备验证前提（当前 narrative 未配置 / 未启用）
- D23-04：通过
- D23-05：部分通过 / 补记完成

### 补充说明
这份记录的价值不在于推翻 D-2.3 已有 review 结论，而在于把页面层现实口径补得更准确：
- route 级 provider 最小闭环：此前已由自动化与本地验证证明
- 页面层 fallback 链路：本次已由真实人工联调证明
- 页面层 provider 命中：当前尚缺真实人工证据，缺口来自运行配置前提未满足，而不是页面已知断裂

---

## 7. 通过判定标准
若满足以下条件，则本记录可回写为“已确认 / 通过”：
1. 页面可正常进入对局并完成至少一回合
2. timeline / narrative 在页面层正常出现
3. provider 命中时页面表现无结构性异常
4. fallback 场景下页面不空白、不报错、不打断主链路

---

## 8. 补记完成后的下一步
本记录完成后，D-2.3 的补充验收证据链已经比此前更完整，但也更精确地暴露了页面层现实缺口。

当前最推荐的直接下一步不是回退旧问题，而是二选一明确：
1. **若目标是补齐 D-2.3 页面层真实 provider 命中证据**：先在后台真正配置并启用 `narrative` 模型，再补一轮极小页面联调，专门验证 D23-03
2. **若不再补 D-2.3 页面层 provider 证据**：则保留本记录“部分通过”结论，随后正式进入下一轮切片判断

不应再回退到：
- D-2.3 是否已收口
- review 是否已形成
- 本地稳定快照是否已提交
- 远端是否已 push

---

## 9. 2026-04-08 客户端补测反馈补记
### 补测背景
在本记录形成后，用户又基于客户端真实页面继续补测了一轮，并给出两条新的主观体验反馈：
1. **对局 AI 大模型的智能感不让人满意**
2. **演绎 AI 的对话质感也不让人满意，怀疑提示词存在问题**

### 本次补记结论
这组反馈**不推翻**本记录前文已经确认的事实：
- 页面主链路可登录、可进入对局、可正常落子
- fallback narrative 在页面层不破链
- route 级最小闭环已由自动化验证证明成立

但它明确把问题重心推进到了新的层面：
- **对局 AI 智能不足**：更需要核查是否真的稳定命中大模型、输入给模型的局面信息是否足够、是否存在 fallback 或降级污染
- **演绎 AI 对话不满意**：更像 prompt / schema / 上下文拼装质量问题，而不只是“有没有接上 provider”

### 对本记录口径的影响
因此，这份页面联调记录的最准确口径应保持为：
> **D-2.3 页面承接与 fallback 语义成立，但页面层命中质量与最终体验表现仍未收口。**

下一轮若继续推进，不应再把目标写成泛泛的“继续测接没接上”，而应专项拆成：
1. 对局 AI 是否真实稳定命中 + 输入是否足够
2. 演绎 AI 的 prompt / schema / 上下文是否导致输出模板味重、棋局锚点弱、像轮流解说

